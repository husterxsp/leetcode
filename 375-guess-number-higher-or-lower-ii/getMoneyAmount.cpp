class Solution {
public:
    int getMoneyAmount(int n) {
        int left = 1, right = n, ret = 0   ;
        while (left < right) {
            int mid = left + (right - left) / 2;

        }
    }
};
